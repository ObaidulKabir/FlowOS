namespace FlowOS.Domain.Enums;

public enum WorkflowContextBindingStatus
{
    Draft = 0,
    Active = 1,
    Archived = 2
}

public enum WorkflowContextBindingRevisionStatus
{
    Draft = 0,
    Active = 1,
    Superseded = 2
}
